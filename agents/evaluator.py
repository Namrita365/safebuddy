class EvaluatorAgent:
    """
    EvaluatorAgent: validates or formats the response.
    """
    def evaluate(self, response: str) -> str:
        # Could add more checks in future (e.g., filter offensive content)
        return response
